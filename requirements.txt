# Image Processor Requirements
# Core GUI framework
PyQt6>=6.4.0

# Image processing libraries
opencv-python>=4.7.0
numpy>=1.21.0
Pillow>=9.0.0

# Adaptive Grading enhancement method. `pip install -r requirements.txt`
# pulls PyPI's default torch wheel, which is typically the CUDA build
# (multiple GB) on Windows/Linux. If you don't have an NVIDIA GPU, install
# the much smaller CPU-only build instead -- see INSTALL.md.
torch>=2.0.0
